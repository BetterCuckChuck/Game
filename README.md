This is a clone of Asteroids for a DSA project.
To play, clone the repository, run `python src_own/main.py` after installing all dependencies.
Alternatively, download the release binaries and run the ./Asteroids executable.